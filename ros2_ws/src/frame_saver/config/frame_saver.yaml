/**:
  ros__parameters:
    image_types: ["rgb", "depth"]
    rgb:
      topics: ["/zed/zed_node/left/color/rect/image"]
      encoding: "bgr8"
    depth:
      topics: ["/zed/zed_node/depth/depth_registered"]
      encoding: "32FC1"
    save_rate: 3.0
    save_dir: "."
    mode: "async"
    timeout: 0.1
    use_sim_time: false
